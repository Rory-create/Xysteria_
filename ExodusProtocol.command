#!/usr/bin/env bash
# Double-click this file on macOS to launch Exodus Protocol.
# On first run: right-click → Open (to bypass Gatekeeper), then double-click normally after that.
cd "$(dirname "$0")"
bash play.sh
